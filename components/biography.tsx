"use client"

interface BiographyProps {
  language: "en" | "es"
}

// This component is now empty as the content moved to Hero
export default function Biography({ language }: BiographyProps) {
  return null
}
